/*Добавьте в этот файл пояснения к выбранной модели данных. 
Также вы можете сохранить здесь другие комментарии по проекту, которые вы считаете необходимыми, 
и задать вопросы ревьюеру*/

/*
    table: raw_data.sales

    auto                 TEXT               - данные в колонке содержат цифры и буквы
    gasoline_consumption DOUBLE PRECISION   - формат подходит для хранения данных чисел с плавающей запятой
    price                NUMERIC(9, 2)      - удобный числовой формат для хранений цен. Позволяет задать точность и масштаб соответствующий требованиям ТЗ
    date                 DATE               - формат подходит для хранения даты без времени
    person_name          TEXT               - формат для хранения текста
    phone                TEXT               - формат для хранения текста. В поле содержатся числа и другие символы
    discount             SMALLINT           - формат для хранения целых чисел
    brand_origin         TEXT               - формат для хранения текста
*/

/*
    table: car_shop.colors

    auto                 TEXT               - данные в колонке содержат цифры и буквы
    gasoline_consumption DOUBLE PRECISION   - формат подходит для хранения данных чисел с плавающей запятой
    price                NUMERIC(9, 2)      - удобный числовой формат для хранений цен. Позволяет задать точность и масштаб соответствующий требованиям ТЗ
    date                 DATE               - формат подходит для хранения даты без времени
    person_name          TEXT               - формат для хранения текста
    phone                TEXT               - формат для хранения текста. В поле содержатся числа и другие символы
    discount             SMALLINT           - формат для хранения целых чисел
    brand_origin         TEXT               - формат для хранения текста
*/

/*
    table: car_shop.colors

    id   SMALLSERIAL - тип данных для создания автоинкремента. Выбран ввиду небольшого количества данных в таблице
    name TEXT        - тип данных для хранения текстовых значений. Выбран т.к при вставке значения в поле с данным типом данных
                       занимает меньше физического места на диске.
*/

/*
    table: car_shop.cars_model

    id                     SMALLSERIAL     - тип данных для создания автоинкремента. Выбран ввиду небольшого количества данных в таблице
    brand_name             TEXT            - тип данных для хранения текстовых значений. Выбран т.к при вставке значения в поле с данным типом данных
                                             занимает меньше физического места на диске.
    model_name             TEXT            - тип данных для хранения текстовых значений. Выбран т.к при вставке значения в поле с данным типом данных
                                             занимает меньше физического места на диске.
    gasoline_consumption   NUMERIC(3, 1)   - удобный числовой формат для чисел с произвольной точностью. Позволяет задать точность и масштаб соответствующий требованиям ТЗ.
    brand_origin           TEXT             - тип данных для хранения текстовых значений. Выбран т.к при вставке значения в поле с данным типом данных
                                             занимает меньше физического места на диске.
*/

/*
    table: car_shop.customer

    id    SMALLSERIAL - тип данных для создания автоинкремента. Выбран ввиду небольшого количества данных в таблице
    name  TEXT        - тип данных для хранения текстовых значений. Выбран т.к при вставке значения в поле с данным типом данных
                        занимает меньше физического места на диске.
    phone TEXT        - тип данных для хранения текстовых значений. Выбран т.к при вставке значения в поле с данным типом данных
                        занимает меньше физического места на диске.
*/

/*
    table: car_shop.auto

    id       SMALLSERIAL - тип данных для создания автоинкремента. Выбран ввиду небольшого количества данных в таблице
    cars_id  SMALLINT    - тип данных для хранения целочисленных значений. Выбран т.к при вставке значения в поле с данным типом данных
                           занимает меньше физического места на диске. А также значения в колонке соответствует данному диапозону -32768 .. +32767
    color_id SMALLINT    - тип данных для хранения целочисленных значений. Выбран т.к при вставке значения в поле с данным типом данных
                           занимает меньше физического места на диске. А также значения в колонке соответствует данному диапозону -32768 .. +32767
*/

/*
    table: car_shop.order_list

    order_id    SMALLSERIAL   - тип данных для создания автоинкремента. Выбран ввиду небольшого количества данных в таблице
    auto_id     SMALLINT      - тип данных для хранения целочисленных значений. Выбран т.к при вставке значения в поле с данным типом данных
                                занимает меньше физического места на диске. А также значения в колонке соответствует данному диапозону -32768 .. +32767
    customer_id SMALLINT      - тип данных для хранения целочисленных значений. Выбран т.к при вставке значения в поле с данным типом данных
                                занимает меньше физического места на диске. А также значения в колонке соответствует данному диапозону -32768 .. +32767
    price       NUMERIC(9,2)  - удобный числовой формат для чисел с произвольной точностью. Позволяет задать точность и масштаб соответствующий требованиям ТЗ.
    discount    SMALLINT      - тип данных для хранения целочисленных значений. Выбран т.к при вставке значения в поле с данным типом данных
                                занимает меньше физического места на диске. А также значения в колонке соответствует данному диапозону -32768 .. +32767
    sale_day    DATE          - формат подходит для хранения даты без времени
*/